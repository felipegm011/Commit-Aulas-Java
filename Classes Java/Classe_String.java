import javax.swing.JOptionPane;
public class Classe_String {

	public static void main(String[] args) {
		
		/*
		// Usando a classe String
		
		String nome = "felipe"; // esse é um objeto por conta da classe string
		int idade = 18;		// essa é uma variavel com um tipo primitivo
		String sobrenome = "gomes";
		
		System.out.println(nome.concat(sobrenome)); // concatena string ou inves de usar +
		System.out.println(nome.toUpperCase());	// converte o texto em MAIUSCULO
		
		 
		//###########################################################################//
		
		
		// usando metodo split para quebrar string
		String data = JOptionPane.showInputDialog("Digite a data dd/mm/aa: ");
		
		String meses[] = {"janeiro","fevereiro","março","abril","maio","junho","julho",
						  "agosto","setembro","outubro","novembro","dezembro"};
		
		// metodo split vai quebrar a string quando houver uma barra
		String parte[] = data.split("/"); 
		
		// variavel vai pegar a mês digitado e transforma em inteiro
		int aux = Integer.parseInt(parte[1]); 
		
		// VARIAVEL VAI RECEBER O ARRAY meses e vai ser acessado pelo valor indexado da varival aux
		String completo = meses[(aux - 1)];	
	
		JOptionPane.showMessageDialog(null, parte[0] + "de"+ completo +"de" + parte[2]);
		*/
	}

}
